# VisionClaw 问题排查表

## 一、APK问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 打开APP转圈 | UI阻塞/初始化问题 | 移除阻塞初始化，延迟连接 |
| 安装后打不开 | 编译错误/代码损坏 | 重新构建，检查代码 |
| 页面跳转失败 | 路由错误/Navigator问题 | 检查页面路径 |

## 二、连接问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| WebSocket连接超时 | IP错误/服务器未运行 | 检查IP，确保后端运行 |
| 404 Not Found | 路由路径错误 | 检查API路径是否为/api/v1 |
| 连接被拒绝 | 防火墙/端口未开放 | 检查端口和防火墙 |
| 连接不稳定 | 网络问题/超时设置 | 增加超时时间 |

## 三、API问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| MiniMax余额不足 | API Key余额用完 | 充值或换用模拟模式 |
| API返回404 | 路由路径错误 | 检查openapi.json确认路径 |
| Redis连接失败 | Redis未运行 | 忽略（可选组件） |
| 数据库错误 | 数据库未配置 | 使用模拟数据模式 |

## 四、Flutter开发问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 编译失败 | 语法错误/导入错误 | flutter analyze检查 |
| duplicate_named_argument | 重复参数 | 删除重复的参数定义 |
| undefined_method | 方法未定义 | 检查方法名和导入 |
| is not true (defunct) | 页面销毁后更新UI | 添加mounted检查 |

## 五、排查命令

```bash
# 1. 检查后端状态
curl http://localhost:8000/api/v1/health

# 2. 检查API路由
curl http://localhost:8000/openapi.json | grep -o '"/api/[^"]*"'

# 3. 测试聊天
curl -X POST http://localhost:8000/api/v1 \
  -H "Content-Type: application/json" \
  -d '{"message":"hello"}'

# 4. 检查本机IP
hostname -I | awk '{print $1}'
```

## 六、预防措施

1. 每次修改前备份代码
2. 用flutter analyze检查后再构建
3. 先本地测试再让用户测试
4. 记录所有已发现的问题
5. 使用模拟数据模式快速验证

## 七、WebSocket连接问题

### 常见原因
| 原因 | 说明 |
|------|------|
| 网络切换 | WiFi切换到4G/5G会导致断开 |
| 服务器重启 | 后端重启后连接失效 |
| 超时 | 长时间无活动被断开 |
| 防火墙 | 运营商/路由器防火墙阻止 |
| IP变化 | 手机IP变化导致连接失效 |

### 解决方案
1. 添加自动重连机制
2. 心跳保活
3. 网络状态监听
4. 错误处理和重试

### Flutter WebSocket最佳实践
```dart
// 1. 在App级别管理连接，不随页面销毁
// 2. 添加重连逻辑
// 3. 监听网络状态变化
```

## 八、当前已知问题

| 问题 | 状态 | 备注 |
|------|------|------|
| MiniMax API余额不足 | ✅ 已解决 | 改用模拟模式 |
| API路径错误 | ✅ 已解决 | /api/v1而非/api/v1/chat |
| 连接超时 | ⚠️ 待优化 | 需添加重连机制 |
| 页面切换断开 | ⚠️ 需排查 | 可能网络问题 |
